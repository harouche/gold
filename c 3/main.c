#include <stdio.h>
#include <stdlib.h>

int main()
{
    float km;
    printf("donner moi la distance en km");
    scanf ("%f",&km);
    float miles =(km * 1093.61);
    printf("la distance en  km est %f ",miles);

    return 0;
}
